# Layout rules for design/diagrams/system-topology.drawio
# Consumed by design/diagrams/generate-topology.js
#
# This file is the single place to edit positions, styles, and pipe routing.
# `system.yaml` is the source of truth for WHAT exists (components, valves,
# sensors, modes). This file decides WHERE things go and HOW pipes connect.
#
# Regenerate after edits:   node design/diagrams/generate-topology.js
#
# Port coordinates are fractional (0..1) relative to the owning cell's
# bounding box: {x: 0, y: 0} is the top-left corner, {x: 1, y: 1} is the
# bottom-right corner.

canvas:
  width: 1600
  height: 980
  background: "#0d1117"

title:
  text: "Greenhouse Solar Heating — Pipe & Valve Topology"
  subtitle: "8 motorized valves · Wilo pump · 2 solar collectors · Jäspi 300L tank · radiator + fan · 2kW space heater"
  color: "#e9c349"
  subtitle_color: "#888888"

# ------------------------------------------------------------------------------
# Named style palette — referenced by components, manifolds, valves, sensors,
# and pipes. Changing a style here updates everything that uses it.
# ------------------------------------------------------------------------------
styles:
  collector:          "rounded=1;whiteSpace=wrap;html=1;fillColor=#f9a82520;strokeColor=#f9a825;strokeWidth=2.5;fontColor=#f9a825;fontSize=12;fontStyle=1;verticalAlign=middle;"
  tank:               "rounded=1;whiteSpace=wrap;html=1;fillColor=#1565c015;strokeColor=#1565c0;strokeWidth=2.5;"
  reservoir:          "rounded=1;whiteSpace=wrap;html=1;fillColor=#42a5f520;strokeColor=#42a5f5;strokeWidth=2.5;fontColor=#64b5f6;fontSize=11;fontStyle=1;"
  pump:               "ellipse;whiteSpace=wrap;html=1;fillColor=#00695c20;strokeColor=#4fc3f7;strokeWidth=3;fontColor=#4fc3f7;fontSize=11;fontStyle=1;"
  radiator:           "rounded=1;whiteSpace=wrap;html=1;fillColor=#ff572220;strokeColor=#ff5722;strokeWidth=2;fontColor=#ff8a65;fontSize=12;fontStyle=1;"
  fan:                "ellipse;whiteSpace=wrap;html=1;fillColor=#ff980030;strokeColor=#ff9800;strokeWidth=2;fontColor=#ffb74d;fontSize=10;fontStyle=1;"
  space_heater:       "rounded=1;whiteSpace=wrap;html=1;fillColor=#f4433620;strokeColor=#f44336;strokeWidth=2;fontColor=#ef9a9a;fontSize=11;fontStyle=1;"
  greenhouse_body:    "rounded=0;whiteSpace=wrap;html=1;fillColor=#2e7d3210;strokeColor=#2e7d32;strokeWidth=2.5;fontColor=#66bb6a;fontSize=16;fontStyle=1;verticalAlign=top;spacingTop=16;"
  greenhouse_roof:    "triangle;whiteSpace=wrap;html=1;direction=north;fillColor=#2e7d3215;strokeColor=#2e7d32;strokeWidth=2.5;"

  # Manifolds
  input_manifold:     "rounded=1;whiteSpace=wrap;html=1;fillColor=#1a2a1a;strokeColor=#388e3c;strokeWidth=1.5;fontColor=#66bb6a;fontSize=12;fontStyle=1;verticalAlign=top;spacingTop=6;"
  output_manifold:    "rounded=1;whiteSpace=wrap;html=1;fillColor=#2a1a1a;strokeColor=#c62828;strokeWidth=1.5;fontColor=#ef5350;fontSize=12;fontStyle=1;verticalAlign=top;spacingTop=6;"
  coll_top_manifold:  "rounded=1;whiteSpace=wrap;html=1;fillColor=#1a1a2e;strokeColor=#555555;strokeWidth=1;fontColor=#b39ddb;fontSize=10;fontStyle=1;verticalAlign=top;spacingTop=6;"

  # Valves (single style for all motorized on/off valves)
  valve:              "rounded=1;whiteSpace=wrap;html=1;fillColor=#e040fb20;strokeColor=#e040fb;strokeWidth=1.5;fontColor=#e040fb;fontSize=10;"

  # Sensors: single 10×10 green dot with label positioned to the right by default.
  # Override per-sensor with `label_side: left` to flip the label to the other side.
  sensor:             "ellipse;whiteSpace=wrap;html=1;fillColor=#76ff03;strokeColor=#76ff03;fontColor=#76ff03;fontSize=9;labelPosition=right;verticalLabelPosition=middle;align=left;verticalAlign=middle;spacingLeft=4;"

  # Pipe styles. The generator appends exitX/exitY/entryX/entryY based on each
  # pipe's resolved from/to ports, so do NOT embed those here.
  pipe_supply:        "endArrow=classic;html=1;strokeColor=#42a5f5;strokeWidth=1.5;fontColor=#42a5f5;fontSize=9;"
  pipe_supply_thick:  "endArrow=classic;html=1;strokeColor=#42a5f5;strokeWidth=3;"
  pipe_hot:           "endArrow=classic;html=1;strokeColor=#ef5350;strokeWidth=2;fontColor=#ef9a9a;fontSize=9;"
  pipe_hot_thick:     "endArrow=classic;html=1;strokeColor=#ef5350;strokeWidth=3;"
  pipe_coll_loop:     "endArrow=classic;html=1;strokeColor=#f9a825;strokeWidth=1.5;fontColor=#f9a825;fontSize=9;"
  pipe_coll_plain:    "endArrow=none;html=1;strokeColor=#f9a825;strokeWidth=2;"
  pipe_drain:         "endArrow=classic;html=1;strokeColor=#1565c0;strokeWidth=1.5;fontColor=#64b5f6;fontSize=9;"
  pipe_rad_supply:    "endArrow=classic;html=1;strokeColor=#ff5722;strokeWidth=2;"
  pipe_dip_dashed:    "endArrow=classic;html=1;strokeColor=#ef5350;strokeWidth=1.5;dashed=1;fontColor=#ef9a9a;fontSize=9;"
  pipe_reservoir_return: "endArrow=classic;html=1;strokeColor=#ef9a9a;strokeWidth=1.5;dashed=1;fontColor=#ef9a9a;fontSize=9;"

# ------------------------------------------------------------------------------
# Theme overrides. The default (dark) theme uses the colors declared in the
# `styles` palette above and on the `canvas.background`. Alternate themes
# declare color substitution maps that the generator applies to every emitted
# `fillColor=`, `fontColor=`, and `strokeColor=` value (alpha suffixes
# preserved). The canvas background is overridden via `canvas_background`.
#
# Invoke a non-default theme via:
#   node design/diagrams/generate-topology.js --theme light --output <path>
#
# The committed `system-topology.drawio` is always produced with the default
# dark theme — the drift test in tests/topology-diagram.test.js enforces this.
# ------------------------------------------------------------------------------
themes:
  light:
    canvas_background: "#ffffff"
    fill:
      "#111111": "#f5f5f5"   # legend box
      "#1a1a2e": "#ede7f6"   # coll_top_manifold
      "#1a2a1a": "#e8f5e9"   # input_manifold
      "#2a1a1a": "#ffebee"   # output_manifold
    font:
      "#76ff03": "#33691e"   # sensor labels — deep green
      "#90caf9": "#1565c0"   # pale blue → tank blue
      "#64b5f6": "#0d47a1"   # pale blue → material blue 900 (contrast on tinted panels)
      "#42a5f5": "#1565c0"   # blue 400 → blue 800
      "#4fc3f7": "#006064"   # cyan 300 → cyan 900
      "#ef9a9a": "#c62828"   # red 200 → red 800
      "#ef5350": "#c62828"   # red 400 → red 800
      "#ffb74d": "#bf360c"   # orange 300 → deep orange 900
      "#ff8a65": "#bf360c"   # deep orange 300 → deep orange 900
      "#f9a825": "#8b6914"   # yellow 800 → dark amber
      "#e9c349": "#8b6914"   # brand gold → dark amber
      "#66bb6a": "#2e7d32"   # green 400 → green 800
      "#b39ddb": "#4527a0"   # deep purple 200 → deep purple 800
      "#e040fb": "#8e24aa"   # purple A200 → purple 700
      "#aaaaaa": "#555555"   # grey mid
      "#888888": "#555555"   # grey mid
      "#cccccc": "#333333"   # grey 300 → grey 800
      "#666666": "#444444"   # grey darker
    stroke:
      "#444444": "#888888"   # legend border (dark grey → mid grey on white)
      "#333333": "#888888"   # scale line
      "#555555": "#888888"   # coll_top_manifold border

# ------------------------------------------------------------------------------
# Height scale (left column) and ground line
# ------------------------------------------------------------------------------
scale:
  label_x: 20
  markers:
    - {label: "280 cm", y: 128}
    - {label: "200 cm", y: 252}
    - {label: "30 cm",  y: 514}
    - {label: "0 cm",   y: 570}
  line:
    x: 80
    y1: 125
    y2: 580

# ------------------------------------------------------------------------------
# Loose text labels (not attached to a component)
# ------------------------------------------------------------------------------
text_labels:
  coll_top_port_label:
    value: "collector top"
    geometry: {x: 220, y: 135, width: 90, height: 15}
    style: "text;html=1;align=left;fontSize=9;fontColor=#aaaaaa;"
  coll_btm_port_label:
    value: "collector bottom"
    geometry: {x: 220, y: 500, width: 110, height: 15}
    style: "text;html=1;align=left;fontSize=9;fontColor=#aaaaaa;"
  reservoir_vent:
    value: "open / vented ↑"
    geometry: {x: 700, y: 152, width: 150, height: 16}
    style: "text;html=1;align=center;fontSize=9;fontColor=#90caf9;"
  water_level:
    value: "water level ~197 cm (communicating vessels)"
    geometry: {x: 880, y: 325, width: 260, height: 16}
    style: "text;html=1;align=left;fontSize=9;fontColor=#42a5f5;"

# ------------------------------------------------------------------------------
# Manifold containers (visual group boxes — valves inside them are declared
# separately in the `valves` section with absolute geometry)
# ------------------------------------------------------------------------------
manifolds:
  input_manifold:
    label: "INPUT MANIFOLD"
    style: input_manifold
    geometry: {x: 230, y: 640, width: 200, height: 180}
  output_manifold:
    label: "OUTPUT MANIFOLD"
    style: output_manifold
    geometry: {x: 660, y: 640, width: 220, height: 180}

# ------------------------------------------------------------------------------
# Components: the top-level structural shapes that pipes connect to.
# Each component has a `geometry` and a `ports` dict (fractional 0..1).
# ------------------------------------------------------------------------------
components:
  collectors:
    label: "Solar Collectors\n(2 × 2m × 1m)\n30–280 cm"
    style: collector
    geometry: {x: 110, y: 125, width: 100, height: 395}
    ports:
      top:    {x: 0.95, y: 0.04}
      bottom: {x: 0.95, y: 0.97}

  tank:
    label: "Jäspi VLM\n300 L\nunpressurized"
    style: tank
    geometry: {x: 700, y: 250, width: 150, height: 317}
    ports:
      dip_port: {x: 0.25, y: 0.99}   # rendered as red dot
      btm_port: {x: 0.65, y: 0.99}   # rendered as blue dot

  reservoir:
    label: "Reservoir\nair separator"
    style: reservoir
    # Sits on top of the tank — communicating vessels means the water level
    # in the reservoir equals the water level inside the tank's dip tube.
    geometry: {x: 700, y: 170, width: 150, height: 60}
    ports:
      left:             {x: 0, y: 0.5}
      right:            {x: 1, y: 0.5}
      bottom:           {x: 0.5, y: 1}
      top:              {x: 0.5, y: 0}
      left_submerged:   {x: 0, y: 0.7}  # below water line — permanent T-joint-to-reservoir pipe terminates here

  pump:
    label: "PUMP\nWilo Z20/4"
    style: pump
    geometry: {x: 500, y: 695, width: 90, height: 70}
    ports:
      in:  {x: 0, y: 0.5}
      out: {x: 1, y: 0.5}

  radiator:
    label: "Radiator\n+ 230V Fan"
    style: radiator
    geometry: {x: 1100, y: 640, width: 140, height: 70}
    ports:
      in:  {x: 0, y: 0.5}
      out: {x: 0, y: 0.85}

  fan:
    label: "FAN"
    style: fan
    geometry: {x: 1260, y: 653, width: 44, height: 44}

  space_heater:
    label: "2 kW\nSpace Heater"
    style: space_heater
    geometry: {x: 1340, y: 640, width: 140, height: 70}

  greenhouse_roof:
    label: ""
    style: greenhouse_roof
    geometry: {x: 1060, y: 340, width: 500, height: 70}

  greenhouse_body:
    label: "Greenhouse"
    style: greenhouse_body
    geometry: {x: 1060, y: 410, width: 500, height: 370}

# ------------------------------------------------------------------------------
# Valves: individual cells with their own geometry and ports.
# Names must match `system.yaml` valves section (the generator warns on mismatch).
# ------------------------------------------------------------------------------
valves:
  # Note: v_air still exists as a physical valve in system.yaml, but its
  # visualization (valve box + pipe to collector top + legend entry) was
  # removed from this layout — the atmosphere intake path is conceptual for
  # drain-mode operation and doesn't need to appear in the topology diagram.
  # generate-topology.js drops v_air from the validateLayout expected set.
  vi_btm:
    label: "VI-btm  (tank btm)"
    geometry: {x: 250, y: 675, width: 160, height: 30}
    ports: {left: {x: 0, y: 0.5}, right: {x: 1, y: 0.5}}
  vi_top:
    label: "VI-top  (reservoir)"
    geometry: {x: 250, y: 715, width: 160, height: 30}
    ports: {left: {x: 0, y: 0.5}, right: {x: 1, y: 0.5}}
  vi_coll:
    label: "VI-coll  (coll btm)"
    geometry: {x: 250, y: 755, width: 160, height: 30}
    ports: {left: {x: 0, y: 0.5}, right: {x: 1, y: 0.5}}

  vo_coll:
    label: "VO-coll  (→ coll btm)"
    geometry: {x: 680, y: 675, width: 180, height: 30}
    ports: {left: {x: 0, y: 0.5}, right: {x: 1, y: 0.5}}
  vo_rad:
    label: "VO-rad  (→ radiator)"
    geometry: {x: 680, y: 715, width: 180, height: 30}
    ports: {left: {x: 0, y: 0.5}, right: {x: 1, y: 0.5}}
  vo_tank:
    label: "VO-tank  (→ tank)"
    geometry: {x: 680, y: 755, width: 180, height: 30}
    ports: {left: {x: 0, y: 0.5}, right: {x: 1, y: 0.5}}

# ------------------------------------------------------------------------------
# Sensors: 10×10 ellipse dots with drawio-attached labels.
# Names must match `system.yaml` sensors section.
# ------------------------------------------------------------------------------
sensors:
  t_collector:
    label: "T_coll"
    geometry: {x: 95, y: 140}
  t_tank_top:
    label: "T_tank_top"
    geometry: {x: 692, y: 290}
    label_side: left
  t_tank_bottom:
    label: "T_tank_btm"
    geometry: {x: 692, y: 525}
    label_side: left
  t_greenhouse:
    label: "T_greenhouse"
    geometry: {x: 1170, y: 500}
  t_outdoor:
    label: "T_outdoor"
    geometry: {x: 1000, y: 400}

# ------------------------------------------------------------------------------
# Pipes: declarative connection list. `from`/`to` reference a component, valve,
# or manifold + a port name. The generator resolves these to vertex IDs and
# exitX/entryX style attributes so moving a component drags its pipes along.
#
# Waypoints (optional) pin specific intermediate points for complex routes.
# ------------------------------------------------------------------------------
pipes:
  # -- Collector loop (top) ----------------------------------------------------
  # Passive T joint at collector top (spec 024): permanent pipe down to the
  # reservoir's submerged port. The V_air branch is implicit in system.yaml
  # but not drawn (see `valves:` note above).
  - id: pipe_coll_top_reservoir
    from: {component: collectors, port: top}
    to:   {component: reservoir, port: left_submerged}
    style: pipe_supply
    waypoints:
      - {x: 650, y: 140}
      - {x: 650, y: 212}

  # -- Reservoir ↔ tank dip tube (gas vent path) -------------------------------
  # Reservoir is now directly above the tank, so the pipe exits the dip port
  # downward out of the tank, wraps around the tank's right side, and enters
  # the reservoir's right port.
  - id: pipe_dip_reservoir
    from: {component: tank, port: dip_port}
    to:   {component: reservoir, port: right}
    style: pipe_dip_dashed
    label: "dip tube port → reservoir (gas vents)"
    waypoints:
      - {x: 737, y: 590}
      - {x: 910, y: 590}
      - {x: 910, y: 200}

  # -- Reservoir → VI-top (de-aired supply for greenhouse heating) -------------
  # Exits the reservoir's left face (bottom port would dive into the tank now
  # that the reservoir sits on top) and routes around the collectors to reach
  # the input manifold.
  - id: pipe_reservoir_vitop
    from: {component: reservoir, port: left}
    to:   {valve: vi_top, port: left}
    style: pipe_reservoir_return
    label: "reservoir bottom → VI-top (de-aired)"
    waypoints:
      - {x: 660, y: 200}
      - {x: 660, y: 550}
      - {x: 185, y: 550}
      - {x: 185, y: 730}

  # -- Tank bottom → VI-btm (cool water supply for solar charging) -------------
  - id: pipe_tank_vibtm
    from: {component: tank, port: btm_port}
    to:   {valve: vi_btm, port: left}
    style: pipe_supply
    waypoints:
      - {x: 800, y: 880}
      - {x: 210, y: 880}
      - {x: 210, y: 690}

  # -- Collector bottom → VI-coll (drain mode supply) --------------------------
  - id: pipe_collbtm_vicoll
    from: {component: collectors, port: bottom}
    to:   {valve: vi_coll, port: left}
    style: pipe_coll_loop
    waypoints:
      - {x: 210, y: 770}

  # -- Input manifold valves → pump input (conceptual single pipe) -------------
  - id: pipe_vibtm_pump
    from: {valve: vi_btm, port: right}
    to:   {component: pump, port: in}
    style: pipe_supply_thick
    waypoints:
      - {x: 450, y: 690}
      - {x: 450, y: 730}
  - id: pipe_vitop_pump
    from: {valve: vi_top, port: right}
    to:   {component: pump, port: in}
    style: pipe_supply_thick
  - id: pipe_vicoll_pump
    from: {valve: vi_coll, port: right}
    to:   {component: pump, port: in}
    style: pipe_supply_thick
    waypoints:
      - {x: 450, y: 770}
      - {x: 450, y: 730}

  # -- Pump output → output manifold valves ------------------------------------
  - id: pipe_pump_vocoll
    from: {component: pump, port: out}
    to:   {valve: vo_coll, port: left}
    style: pipe_hot_thick
    waypoints:
      - {x: 640, y: 730}
      - {x: 640, y: 690}
  - id: pipe_pump_vorad
    from: {component: pump, port: out}
    to:   {valve: vo_rad, port: left}
    style: pipe_hot_thick
  - id: pipe_pump_votank
    from: {component: pump, port: out}
    to:   {valve: vo_tank, port: left}
    style: pipe_hot_thick
    waypoints:
      - {x: 640, y: 730}
      - {x: 640, y: 770}

  # -- Output manifold → destinations ------------------------------------------
  - id: pipe_vocoll_collbtm
    from: {valve: vo_coll, port: right}
    to:   {component: collectors, port: bottom}
    style: pipe_coll_loop
    label: "supply to collectors"
    waypoints:
      - {x: 895, y: 690}
      - {x: 895, y: 615}
      - {x: 235, y: 615}

  - id: pipe_vorad_radiator
    from: {valve: vo_rad, port: right}
    to:   {component: radiator, port: in}
    style: pipe_rad_supply
    waypoints:
      - {x: 1080, y: 730}
      - {x: 1080, y: 675}

  - id: pipe_votank_tank
    from: {valve: vo_tank, port: right}
    to:   {component: tank, port: btm_port}
    style: pipe_drain
    label: "drain → tank bottom"
    waypoints:
      - {x: 870, y: 770}
      - {x: 870, y: 590}

  # -- Radiator return → tank bottom port --------------------------------------
  - id: pipe_rad_return
    from: {component: radiator, port: out}
    to:   {component: tank, port: btm_port}
    style: pipe_supply
    label: "radiator return → tank bottom"
    waypoints:
      - {x: 1040, y: 700}
      - {x: 1040, y: 820}
      - {x: 920, y: 820}
      - {x: 920, y: 582}

# ------------------------------------------------------------------------------
# Legend (top-right box). Items are rendered top-to-bottom in declaration order.
# ------------------------------------------------------------------------------
legend:
  position: {x: 1360, y: 90, width: 220, height: 140}
  items:
    - {type: line,    color: "#42a5f5", width: 2.5, label: "Supply / cool water"}
    - {type: line,    color: "#ef5350", width: 2.5, label: "Output / hot water"}
    - {type: line,    color: "#f9a825", width: 2,   label: "Collector loop"}
    - {type: swatch,  fill: "#e040fb20", stroke: "#e040fb", label: "Motorized on/off valve"}
    - {type: dot,     color: "#76ff03", label: "DS18B20 sensor"}
