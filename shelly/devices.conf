# Device IP addresses — DHCP reservations on router
# Used by deploy.sh

PRO4PM=192.168.1.174

# Pro 2PM valve controllers (no scripts deployed, listed for reference)
# IP assignments match VALVES map in control.js
PRO2PM_1=192.168.1.11  # unit_1: vi_btm, vi_top (input manifold)
PRO2PM_2=192.168.1.12  # unit_2: vi_coll, vo_coll (input/output manifold)
PRO2PM_3=192.168.1.13  # unit_3: vo_rad, vo_tank (output manifold)
PRO2PM_4=192.168.1.14  # unit_4: v_ret, v_air (collector top)

# Shelly 1 sensor hub (sensor add-on with DS18B20)
SENSOR=192.168.1.86

# VPN-reachable IPs (same devices, accessed from cloud server via VPN tunnel)
PRO4PM_VPN=${CONTROLLER_VPN_IP:-192.168.1.174}
