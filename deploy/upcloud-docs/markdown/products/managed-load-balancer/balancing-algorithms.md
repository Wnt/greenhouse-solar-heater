# Managed Load Balancer Balancing Algorithms

Managed Load Balancer currently supports *round robin* only. Other algorithm options will be added later. Round robin uses each backend server evenly in rotation.
