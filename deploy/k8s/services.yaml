apiVersion: v1
kind: Service
metadata:
  name: app
  labels:
    app: greenhouse
spec:
  type: ClusterIP
  selector:
    app: greenhouse
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
